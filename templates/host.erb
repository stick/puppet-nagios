define host {
    use                         <%= template %>
    host_name                   <%= fqdn %>
    address                     <%= ipaddress %>
    hostgroups                  <%= host_groups %>
    alias                       <%= nagios_alias %>
    contact_groups              <%= contact_groups %> 
    parents                     <%= parents %>
}
