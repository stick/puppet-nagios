define host {
    use                         <%= template %>
    host_name                   <%= fqdn %>
    address                     <%= ipaddress %>
    alias                       <%= alias %>
    contact_groups              <%= contact_groups %> 
    parents                     <%= parents %>
}
